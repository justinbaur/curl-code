# Sample .env file for REST Client $dotenv testing
# REST Client expects this in the same directory as the .http file

API_HOST=httpbin.org
API_TOKEN=test-token-abc123
REQUEST_PREFIX=test
DATA_SOURCE=manual-import
DB_CONNECTION=postgres://user:pass@localhost:5432/testdb
